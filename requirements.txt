# TF 2.15 is the last version that works with the prebuilt wheels below;
# numpy 2.x breaks them, hence the pin.
numpy<2
tensorflow==2.15.1        # tensorflow-cpu if you have no GPU
opencv-python-headless>=4.8
scikit-learn>=1.3
scipy>=1.10
pandas>=2.0
matplotlib>=3.7
tqdm

# optional
ultralytics>=8.3          # YOLO person detection for the keyframe stage
timeshap>=1.0             # reference TimeSHAP; falls back to sampling if absent
