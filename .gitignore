__pycache__/
*.pyc
.ipynb_checkpoints/
.venv/
venv/

# artefacts
ckpt/
features/
logs/
*.h5
*.keras
*.npy
*.pt

# Result files are small and are what make_tables.py reads to produce every
# table in the paper, so they are tracked. Scratch files from smoke runs are
# not.
results/_*.json

# Datasets never go in the repo. Anchored to the repo root with a leading
# slash: a bare `data/` also matches src/data/, which silently excluded the
# dataset loader package from the first push.
/data/
/datasets/

.DS_Store
