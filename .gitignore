__pycache__/
*.pyc
.ipynb_checkpoints/
.venv/
venv/

# artefacts
ckpt/
features/
logs/
results/*.json
!results/.gitkeep
*.h5
*.keras
*.npy
*.pt

# Datasets never go in the repo. Anchored to the repo root with a leading
# slash: a bare `data/` also matches src/data/, which silently excluded the
# dataset loader package from the first push.
/data/
/datasets/

.DS_Store
